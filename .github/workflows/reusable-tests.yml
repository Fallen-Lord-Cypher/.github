name: Zentrale Logik
on:
  workflow_call:

jobs:
  test-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Verbindungs-Check
        run: |
          echo "✅ Verbindung erfolgreich!"
          echo "Der zentrale Workflow wurde vom Projekt-Repo aufgerufen."
          echo "Java-Version auf dem Server ist:"
          java -version